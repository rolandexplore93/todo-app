Steps:
// Create a form input where a user can add a task
// Creata a submit button with the form to add user's input to where it will be displayed on the UI
// Create listItems for the tasks submitted by users
// Allow user to edit their tasks
// Allow user to delete their tasks
// Show the presence of animation when the user submit the form and delete a task
// Deployment